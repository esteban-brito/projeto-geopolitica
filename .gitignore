node_modules/
*.log
!docs/evidence/**/*.log

# Artefatos de medicao e capturas — evidencia, nao fonte.
tmp/
# ⚠ `captures/*` e nao `captures/`: o git nao reinclui arquivo dentro de pasta excluida,
# e o mapa da pasta precisa ser versionado.
captures/*
!captures/README.md

# Material de origem de assets, como no draft9-0: entra grande, sai otimizado.
assets-source/

# O `settings.local.json` e a preferencia da maquina, e nao do projeto.
.claude/settings.local.json

# Artefatos do navegador de medicao — mesma familia de `tmp/`: evidencia, nao fonte.
.playwright-mcp/
