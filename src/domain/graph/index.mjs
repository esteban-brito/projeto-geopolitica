/* DELTA — catalogo de ligacoes, estado e deltas viram nos e arestas com peso e sinal. */

/* Isso nao e otimizacao: `backdrop-filter` sobre fundo em movimento obriga o compositor a
   reamostrar o que esta atras a cada quadro, e foi essa condicao que derrubou uma tela para
   31 fps no projeto anterior. */

export {};
